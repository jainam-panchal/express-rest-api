version: '3.1'

services:
  mongo:
    image: mongo:latest
    container_name: mongo
    ports:
      - '27017:27017'

  redis:
    image: redis/redis-stack-server:latest
    container_name: redis
    ports:
      - '6379:6379'
